# HopperRails プラグイン実装詳細

## バージョン
0.0.1

## 対応バージョン
Minecraft 1.21.8 (Spigot/Paper)

## プラグインの仕組み

### 状態管理システム

このプラグインは、ホッパー付きトロッコを追跡し、3つのモード（状態）で管理します：

#### 1. 待機モード (WAITING)
- ホッパー付きトロッコがチェストの下にいて、アイテムの吸収を待っている状態
- 初期状態
- 移動モードから戻ってきた場合にもこの状態に遷移

#### 2. 吸収モード (ABSORBING)
- ホッパー付きトロッコがチェストからアイテムを吸収している状態
- トロッコのインベントリ内のアイテム数が増加している場合に判定
- 待機モードから遷移

#### 3. 移動モード (MOVING)
- アイテムの吸収が完了し、トロッコが移動する状態
- この状態に遷移する際、レールの下のブロックにレッドストーン信号を送信
- 吸収モードから遷移
- チェストエリアを離れて再度戻ってくると、待機モードへ遷移

### 状態遷移図

```
[待機モード] --アイテム吸収開始--> [吸収モード]
     ^                                    |
     |                              アイテム吸収完了
     |                                    |
     |                                    v
     +--チェストに戻る------ [移動モード]
```

### レッドストーン信号の送信

- 吸収モードから移動モードへ遷移する際、トロッコの位置のレールの下のブロックを一時的にレッドストーンブロックに変更
- デフォルトで20ティック（1秒）後に元のブロックに戻す
- 設定ファイルで持続時間を変更可能

### チェック頻度

- デフォルトで10ティック（0.5秒）ごとにすべてのホッパー付きトロッコをチェック
- 設定ファイルで変更可能

## ファイル構成

### Java クラス

1. **HopperRails.java** - メインプラグインクラス
   - プラグインの有効化/無効化
   - 設定ファイルの読み込み
   - MinecartTrackerの初期化

2. **MinecartTracker.java** - トロッコ追跡クラス
   - すべてのホッパー付きトロッコを定期的にチェック
   - 状態管理とモード遷移ロジック
   - レッドストーン信号の送信
   - トロッコの破壊時のクリーンアップ

3. **MinecartMode.java** - モード定義列挙型
   - WAITING（待機）
   - ABSORBING（吸収）
   - MOVING（移動）

### 設定ファイル

1. **plugin.yml** - プラグインメタデータ（日本語）
2. **config.yml** - 動作設定（日本語）

## 技術的な詳細

### アイテムカウント方式
- ホッパー付きトロッコのインベントリ内のすべてのアイテムをカウント
- 前回のチェック時のアイテム数と比較して吸収中かどうかを判定

### チェストの検出
- トロッコの位置の上1ブロックにチェストがあるかを確認
- BlockFace.UPを使用して上のブロックを取得

### 非同期タスク
- Bukkitのスケジューラーを使用して定期的にチェック
- メインスレッドで実行

### メモリ管理
- 各トロッコをUUIDで追跡
- トロッコが破壊されたらイベントリスナーで自動的にマップから削除

## 設定項目

### デバッグモード
詳細なログを出力するかどうか

### レッドストーン信号持続時間
レッドストーン信号を送信する時間（ティック単位）
デフォルト: 20ティック（1秒）

### チェック間隔
トロッコの状態をチェックする頻度（ティック単位）
デフォルト: 10ティック（0.5秒）

## ビルド要件

- Java 17 以上
- Gradle 8.5
- Spigot API 1.21.8-R0.1-SNAPSHOT

## 使用ライブラリ

- Spigot API 1.21.8-R0.1-SNAPSHOT (compileOnly)
